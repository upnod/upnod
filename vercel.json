{
  "version": 2,
  "routes": [
    { "src": "/img/(.*)", "dest": "/img/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "builds": [
    { "src": "index.html", "use": "@vercel/static" }
  ]
}
